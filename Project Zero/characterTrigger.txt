trigger characterTrigger on Character__c (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    switch on trigger.operationtype {
        when before_insert {
            
        }
        when before_update {
            
        }
        when before_delete {
            charTriggerHandler.weStillNeedYou(trigger.old);
        }
        when after_insert {
            
        }
        when after_update {
            
        }
        when after_delete {
            
        }
        when after_undelete {
            
        }
    }
}